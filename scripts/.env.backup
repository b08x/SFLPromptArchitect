# =============================================================================
# Database Backup Configuration
# =============================================================================
# Copy this file to .env.backup and customize for your environment
# Source this file before running backup.sh: source .env.backup && ./backup.sh

# Database Connection (required)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=sfl_db

# Backup Configuration
BACKUP_PATH=/home/b08x/Workspace/Syncopated/SFL-Prompt-Studio/backups
BACKUP_RETENTION_DAYS=30

# Docker-specific Configuration (for Strategy B - Host Cron)
DOCKER_COMPOSE_PATH=/home/b08x/Workspace/Syncopated/SFL-Prompt-Studio
POSTGRES_CONTAINER_NAME=sfl-db

# Optional: Cloud storage upload (uncomment and configure if needed)
# AWS_PROFILE=default
# S3_BUCKET=sfl-backups
# S3_PREFIX=postgresql/
# BACKUP_ENCRYPTION_KEY=/path/to/encryption.key